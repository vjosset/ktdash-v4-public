generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "mysql"
  url      = env("DATABASE_URL")
}

model User {
  userId    String   @id @db.VarChar(20)
  email     String?  @db.VarChar(250)
  userName  String   @unique @db.VarChar(250)
  password  String   @db.VarChar(250)
  createdAt DateTime @default(now())
  eloRating Float    @default(1000)

  rosters   Roster[]
  killteams Killteam[]
  votes     KillteamVote[]
}

model WebEvent {
  eventId     Int      @id @default(autoincrement())
  datestamp   DateTime @default(now())
  sessionType String   @db.VarChar(50)
  userId      String?  @db.VarChar(45)
  eventType   String?  @db.VarChar(50)
  action      String?  @db.VarChar(45)
  label       String?  @db.VarChar(45)
  var1        String?  @db.VarChar(45)
  var2        String?  @db.VarChar(45)
  var3        String?  @db.VarChar(45)
  url         String   @db.VarChar(500)
  userIp      String   @db.VarChar(250)
  userAgent   String   @db.VarChar(500)
  referrer    String?  @db.VarChar(500)

  @@index([userId, eventType, action, label], name: "IX_TAL")
  @@index([var1, eventType, action, label], name: "IX_VAR1")
  @@index([userId], name: "IX_USER")
  @@index([datestamp], name: "IX_datestamp")
}

model Faction {
  factionId   String @id @db.VarChar(10)
  factionName String @db.VarChar(250)
  description String @db.Text
  seq         Int    @default(0)

  killteams Killteam[]
}

model Killteam {
  killteamId      String  @id @db.VarChar(20)
  factionId       String  @db.VarChar(10)
  seq             Int     @default(0)
  killteamName    String  @db.VarChar(250)
  description     String  @db.Text
  composition     String  @db.Text
  archetypes      String? @db.VarChar(250)
  userId          String? @db.VarChar(20)
  isPublished     Boolean @default(true)
  defaultRosterId String? @unique @db.VarChar(20)
  eloRating       Float   @default(1000)

  faction       Faction?       @relation(fields: [factionId], references: [factionId])
  user          User?          @relation(fields: [userId], references: [userId])
  // Default roster pointer; disambiguated relation name
  defaultRoster Roster?        @relation("KillteamDefaultRoster", fields: [defaultRosterId], references: [rosterId], onDelete: SetNull)
  opTypes       OpType[]
  // All rosters for this killteam; disambiguated relation name
  rosters       Roster[]       @relation("RosterBelongsToKillteam")
  ploys         Ploy[]
  equipments    Equipment[]
  votes         KillteamVote[]

  @@index([defaultRosterId], name: "IX_Killteam_DefaultRoster")
}

model OpType {
  opTypeId   String @id @db.VarChar(30)
  killteamId String @db.VarChar(20)
  seq        Int    @default(0)
  opTypeName String @db.VarChar(250)
  MOVE       String @db.VarChar(10)
  APL        Int    @default(0)
  SAVE       String @db.VarChar(10)
  WOUNDS     Int    @default(0)
  keywords   String @db.VarChar(250)
  basesize   Int    @default(32)
  nameType   String @default("") @db.VarChar(50)

  weapons   Weapon[]
  abilities Ability[]
  options   Option[]
  killteam  Killteam? @relation(fields: [killteamId], references: [killteamId])
  ops       Op[]
}

model Weapon {
  wepId     String  @id @db.VarChar(40)
  opTypeId  String  @db.VarChar(30)
  seq       Int     @default(0)
  wepName   String  @db.VarChar(250)
  wepType   String  @db.VarChar(1)
  isDefault Boolean @default(false)

  profiles WeaponProfile[]
  opType   OpType?         @relation(fields: [opTypeId], references: [opTypeId], onDelete: Cascade)
}

model WeaponProfile {
  wepprofileId String  @id @db.VarChar(50)
  wepId        String  @db.VarChar(40)
  seq          Int     @default(0)
  profileName  String  @db.VarChar(250)
  ATK          String  @db.VarChar(10)
  HIT          String  @db.VarChar(10)
  DMG          String  @db.VarChar(10)
  WR           String  @db.VarChar(250)
  weapon       Weapon? @relation(fields: [wepId], references: [wepId], onDelete: Cascade)
}

model Ability {
  abilityId     String  @id @db.VarChar(50)
  opTypeId      String  @db.VarChar(30)
  abilityName   String  @db.VarChar(250)
  description   String  @db.Text
  AP            Int?
  isFactionRule Boolean @default(false)
  opType        OpType? @relation(fields: [opTypeId], references: [opTypeId], onDelete: Cascade)
}

model WeaponRule {
  code        String  @id @db.VarChar(20)
  ruleName    String  @db.VarChar(250)
  description String? @db.Text
}

model Roster {
  rosterId          String   @id @db.VarChar(20)
  userId            String   @db.VarChar(20)
  seq               Int      @default(0)
  killteamId        String   @db.VarChar(20)
  rosterName        String   @db.VarChar(250)
  description       String?  @db.Text
  turn              Int      @default(1)
  VP                Int      @default(0)
  CP                Int      @default(3)
  hasCustomPortrait Boolean  @default(false)
  isSpotlight       Boolean  @default(false)
  viewCount         Int      @default(0)
  importCount       Int      @default(0)
  createdAt         DateTime @default(now())
  updatedAt         DateTime @updatedAt
  portraitUpdatedAt DateTime @default(now())
  eqIds             String   @default("")
  ployIds           String   @default("")
  eloRating         Float    @default(1000)

  ops                Op[]
  user               User?     @relation(fields: [userId], references: [userId], onDelete: Cascade)
  // Back-reference for rosters collection on Killteam
  killteam           Killteam? @relation("RosterBelongsToKillteam", fields: [killteamId], references: [killteamId], onDelete: Cascade)
  // Back-reference for defaultRoster on Killteam
  defaultForKillteam Killteam?     @relation("KillteamDefaultRoster")
  matchesAsA         MatchResult[] @relation("MatchRosterA")
  matchesAsB         MatchResult[] @relation("MatchRosterB")
}

model Op {
  opId              String   @id @db.VarChar(20)
  rosterId          String   @db.VarChar(20)
  opTypeId          String   @db.VarChar(30)
  seq               Int      @default(0)
  description       String?  @db.Text
  opName            String   @db.VarChar(250)
  currWOUNDS        Int      @default(0)
  isActivated       Boolean  @default(false)
  opOrder           String   @default("concealed") @db.VarChar(10)
  wepIds            String   @db.VarChar(500)
  optionIds         String   @db.VarChar(500)
  hasCustomPortrait Boolean  @default(false)
  isDeployed        Boolean  @default(true)
  createdAt         DateTime @default(now())
  updatedAt         DateTime @updatedAt
  portraitUpdatedAt DateTime @default(now())

  roster Roster? @relation(fields: [rosterId], references: [rosterId], onDelete: Cascade)
  opType OpType? @relation(fields: [opTypeId], references: [opTypeId], onDelete: Cascade)
}

model Option {
  optionId    String @id @db.VarChar(50)
  opTypeId    String @db.VarChar(30)
  seq         Int    @default(0)
  optionName  String @db.VarChar(50)
  description String @db.Text
  effects     String @db.VarChar(50)

  opType OpType? @relation(fields: [opTypeId], references: [opTypeId], onDelete: Cascade)
}

model Equipment {
  eqId        String  @id @db.VarChar(50)
  killteamId  String? @db.VarChar(20)
  seq         Int     @default(0)
  eqName      String  @db.VarChar(250)
  description String  @db.Text
  effects     String  @db.VarChar(250)

  killteam Killteam? @relation(fields: [killteamId], references: [killteamId], onDelete: Cascade)
}

model Ploy {
  ployId      String @id @db.VarChar(50)
  killteamId  String @db.VarChar(20)
  seq         Int    @default(0)
  ployType    String @db.VarChar(50)
  ployName    String @db.VarChar(250)
  description String @db.Text
  effects     String @db.VarChar(250)

  killteam Killteam? @relation(fields: [killteamId], references: [killteamId], onDelete: Cascade)
}

model KillteamVote {
  voteId     Int      @id @default(autoincrement())
  killteamId String   @db.VarChar(20)
  userId     String   @db.VarChar(20)
  value      Int
  createdAt  DateTime @default(now())
  updatedAt  DateTime @updatedAt

  killteam Killteam @relation(fields: [killteamId], references: [killteamId], onDelete: Cascade)
  user     User     @relation(fields: [userId], references: [userId], onDelete: Cascade)

  @@unique([killteamId, userId], map: "IX_KillteamVote_UserPerTeam")
}

model MatchResult {
  matchResultId    Int      @id @default(autoincrement())
  rosterAId        String   @db.VarChar(20)
  rosterBId        String   @db.VarChar(20)
  result           String   @db.VarChar(1)
  rosterAConfirmed Boolean  @default(true)
  rosterBConfirmed Boolean  @default(false)
  matchDate        DateTime @default(now())
  eloBeforeA       Float?
  eloBeforeB       Float?
  eloAfterA        Float?
  eloAfterB        Float?

  rosterA Roster @relation("MatchRosterA", fields: [rosterAId], references: [rosterId], onDelete: Cascade)
  rosterB Roster @relation("MatchRosterB", fields: [rosterBId], references: [rosterId], onDelete: Cascade)

  @@index([rosterAId], name: "IX_MatchResult_RosterA")
  @@index([rosterBId], name: "IX_MatchResult_RosterB")
}
